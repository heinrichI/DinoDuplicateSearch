pip install -U torch torchvision transformers pillow
# optional, for the later examples:
pip install -U numpy scikit-learn faiss-cpu

# For desktop GUI application:
pip install kivy opencv-python

# Run the Kivy app:
python app.py
